/* O Grande Código da Gralha Azul — stub de compilação.
   Toda a implementação vive no header. Este .cpp existe apenas
   para o Arduino reconhecer a biblioteca — NÃO inclui o header
   pois os #define do sketch não são visíveis nesta TU. */